# Unlock the World

## Starting a Development Server

Make sure you are using `node 20`

```bash
npm i # install the dependencies
npx expo run:ios # build IOS assets
```

Make sure you are in `development mode` and not `expo mode` (Press `s`)

Press `i` to start the app on emulator
